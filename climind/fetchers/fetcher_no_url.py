def fetch(url, outdir):
    """
    A stub so that there is a fetcher for datasets which don't exist online
    Parameters
    ----------
    url : str
        Dummy
    outdir : str
        Dummy
    Returns
    -------

    """
    return
